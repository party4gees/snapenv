{
  "name": "snapenv",
  "version": "0.1.0",
  "description": "Snapshot and restore local environment variables across projects",
  "main": "src/index.js",
  "bin": {
    "snapenv": "./bin/snapenv.js"
  },
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "prepare": "node -e \"require('fs').chmodSync('./bin/snapenv.js', '755')\""
  },
  "keywords": ["env", "dotenv", "snapshot", "cli", "environment"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/*.test.js"],
    "collectCoverageFrom": ["src/**/*.js"]
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
